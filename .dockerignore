# Secrets — never bake into the image
.env
*.env

# Python / local environment
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Git
.git/
.gitignore

# Claude Code memory (not part of the app)
.claude/
memory/

# Docker files themselves (not needed inside the image)
Dockerfile
docker-compose.yml
.dockerignore
